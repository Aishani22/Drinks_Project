<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="styles.css">
    <title>Let's Have a Drink!</title>
    <link rel="icon" href="images/drink_pic.jpg" type="image/jpg">
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand">Let's Have a Drink!🥂</a>
            <form class="d-flex" action="/submit" method="POST">
                <input class="form-control me-2" name = "drink" type="text" placeholder="Search" aria-label="Search">
                <button class="btn button-1 btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </nav>

    <div class = "main">
        <div class = "left container">
            <% if(locals.image) { %>
                <img class = "image" src= <%= image %>>
            <% } %>
            <% if (locals.errorMessage) { %>
                <img class = "error_img" src= "images/404_page_cover.jpg">
            <% } %>
        </div>
        <div class = "right container">
            <!-- <div class = "btn-container">
                <form class="d-flex-2" action="/submit" method="GET">
                    <button class="btn btn-outline-success" type="submit">Random Drink</button>
                </form>
            </div> -->
            <% if(locals.cocktail && locals.type && locals.glassType && locals.recipe && locals.instruction) { %>
                <h2><%= cocktail %></h2>
                <p class = "p"><span>Type : </span>  <%= type %></p>
                <br>
                <p class = "p"><span>Glass Type : </span>   <%= glassType %></p>
                <br>
                <h4 class = "h3">Ingredients and their measure :</h4>
                <ul>
                    <% for(let item of recipe) { %>
                        <li><%= item %></li>
                    <% } %>
                </ul>

                <h4 class = "h3">Instructions : </h4><p><%= instruction %></p>
            <% } %>
            <% if (locals.errorMessage) { %>
                <h2 class="error"><%= errorMessage %></h2>
            <% } %>
        </div>
        <div class = "btn-container">
            <form class = "d-flex-2" action="/submit" method="GET">
                <button class="btn button-2 btn-outline-success" type="submit">Random Drink</button>
            </form>
        </div>
    </div>
    <%- include("footer.ejs") %>
</body>
</html>